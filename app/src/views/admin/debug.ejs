<header class="page-header">
  <h1>RBAC Debug</h1>
  <p class="muted">Active user: <strong><%= activeUser?.username || '—' %></strong> · Cookie fp_user: <code><%= activeUserCookie || '—' %></code></p>
</header>

<section class="card">
  <h3 class="card-title">Users</h3>
  <table>
    <thead>
      <tr>
        <th>Username</th>
        <th>Display name</th>
        <th>ID</th>
      </tr>
    </thead>
    <tbody>
      <% users.forEach((item) => { %>
        <tr>
          <td><%= item.username %></td>
          <td><%= item.displayName %></td>
          <td><code><%= item.id %></code></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</section>

<section class="card">
  <h3 class="card-title">Groups</h3>
  <table>
    <thead>
      <tr>
        <th>Key</th>
        <th>Name</th>
        <th>ID</th>
      </tr>
    </thead>
    <tbody>
      <% groups.forEach((item) => { %>
        <tr>
          <td><code><%= item.key %></code></td>
          <td><%= item.name %></td>
          <td><code><%= item.id %></code></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</section>

<section class="card">
  <h3 class="card-title">Memberships</h3>
  <table>
    <thead>
      <tr>
        <th>Group</th>
        <th>User</th>
        <th>Rights</th>
      </tr>
    </thead>
    <tbody>
      <% memberships.forEach((item) => { %>
        <tr>
          <td><code><%= item.groupKey %></code></td>
          <td><code><%= item.username %></code></td>
          <td><code><%= item.rights %></code></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</section>

<section class="card">
  <h3 class="card-title">Template Assignments</h3>
  <table>
    <thead>
      <tr>
        <th>Template</th>
        <th>Group</th>
      </tr>
    </thead>
    <tbody>
      <% assignments.forEach((item) => { %>
        <tr>
          <td><%= item.templateName %> (<code><%= item.templateKey %></code>)</td>
          <td><code><%= item.groupKey %></code></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</section>

<section class="card">
  <h3 class="card-title">Latest Documents</h3>
  <table>
    <thead>
      <tr>
        <th>Document</th>
        <th>Template</th>
        <th>Group</th>
        <th>Status</th>
      </tr>
    </thead>
    <tbody>
      <% latestDocuments.forEach((item) => { %>
        <tr>
          <td><a href="/documents/<%= item.id %>"><code><%= String(item.id).slice(0, 8) %></code></a></td>
          <td><code><%= item.templateKey %></code></td>
          <td><code><%= item.groupKey %></code></td>
          <td><span class="badge badge-status"><%= item.status %></span></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</section>
